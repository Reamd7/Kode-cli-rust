---
description: Generate commit message from staged changes and create commit.
argument-hint: [additional-context]
---

**任务说明**
根据暂存区的变更生成符合项目规范的 commit message，并自动提交。

**执行步骤**

1. **检查暂存区状态**
   - 运行 `git status` 查看是否有已暂存的文件
   - 如果没有暂存的文件，提示用户先使用 `git add` 暂存文件

2. **分析暂存区变更**
   - 运行 `git diff --cached` 查看暂存区的详细变更
   - 运行 `git diff --cached --stat` 查看变更统计
   - 如果提供了 `$ARGUMENTS`，将其作为额外的上下文考虑

3. **生成 Commit Message**
   根据变更内容和项目规范，生成符合以下格式的 commit message：

   ```
   <type>(<scope>): <subject>

   <body>

   <footer>
   ```

   **类型 (type)**:
   - `feat`: 新功能
   - `fix`: 修复 bug
   - `docs`: 文档变更
   - `style`: 代码格式（不影响代码运行的变动）
   - `refactor`: 重构（既不是新增功能，也不是修改 bug 的代码变动）
   - `perf`: 性能优化
   - `test`: 增加测试
   - `chore`: 构建过程或辅助工具的变动
   - `ci`: CI 配置文件和脚本的变动

   **格式要求**:
   - subject: 不超过 50 字符，使用祈使句，首字母小写，结尾不加句号
   - body: 详细描述 what 和 why，而不是 how，每行不超过 72 字符
   - footer: 关联的 issue，breaking changes 等

4. **创建提交**
   - 使用生成的 commit message 创建提交
   - Commit message 必须包含以下内容：
     - 清晰的 type 和 scope
     - 简洁的 subject
     - 详细的 body（说明变更的原因和内容）
     - 标准的 footer：
       ```
       🤖 Generated with [Claude Code](https://claude.com/claude-code)

       Co-Authored-By: Claude <noreply@anthropic.com>
       ```

5. **验证提交**
   - 运行 `git log -1 --format=fuller` 显示刚创建的提交
   - 运行 `git status` 确认提交成功

**示例**

```
# 场景：添加了配置加载功能
feat(config): 实现配置文件加载和解析

- 添加 Config 结构体和相关类型定义
- 实现 JSON 格式配置文件解析
- 支持配置文件验证和错误处理
- 添加单元测试覆盖主要逻辑

Closes #123

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

**注意事项**
- 如果变更涉及多个不相关的功能，建议分别提交
- 如果不确定 commit message 的格式，可以先显示生成的 message 让用户确认
- 确保 body 部分清晰说明变更的原因和内容
- 对于 breaking changes，在 footer 中明确标注

$ARGUMENTS
